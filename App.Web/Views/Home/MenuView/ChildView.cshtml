@model IEnumerable<App.Model.Menu.MenuItem>
@if (Model.Count() > 0)
{
    <ul class="dropdown">
        @foreach (var item in Model)
        {
            <li><a href="@item.UrlString">@item.NameCategory</a></li>
        }
    </ul>
}
